name: say hello
on: push
jobs:
  sample-job:
    runs-on: ubuntu-latest
    steps:
      - name: first step
        run: echo "Hello World!"
      - name: second step
        run: echo "Hello React!"
